---
import { booking, bookingCta, contact } from '../data/site';

/**
 * The booking section. Deliberately loud — this is the thing the page exists
 * to get people to do.
 *
 * Two states:
 *   1. booking.url is set  -> "book online through Vagaro" + big button
 *   2. booking.url is ''   -> falls back to call/email, and does NOT claim
 *                             online booking exists
 * See the notes on `booking` in src/data/site.ts.
 */
const configured = Boolean(booking.url);
---

<section class="booking" id="book">
  <div class="container booking__inner" data-reveal>
    <span class="eyebrow">Booking</span>

    {
      configured ? (
        <>
          <h2 class="booking__title">{bookingCta.label}</h2>
          <p class="booking__text">
            All classes are booked online through {booking.provider} — new
            students and existing members alike. Pick your class, reserve your
            spot, and turn up.
          </p>

          <a
            href={booking.url}
            class="btn btn--primary btn--xl"
            target="_blank"
            rel="noopener noreferrer"
          >
            {bookingCta.label}
          </a>

          {/* The free trial, kept as a quiet line under the button */}
          <p class="booking__trial">
            New here?{' '}
            <a href={booking.url} target="_blank" rel="noopener noreferrer">
              {bookingCta.trialNote}
            </a>
            .
          </p>

          <p class="booking__note">
            Already a member? Use the same link to book, rebook, or refresh your
            schedule.
          </p>
        </>
      ) : (
        <>
          <h2 class="booking__title">{bookingCta.label}</h2>
          <p class="booking__text">
            Call or email and we'll get you booked in. It takes a minute — and
            if you're new, your first class is free.
          </p>

          <div class="btn-row booking__btns">
            <a href={contact.phoneHref} class="btn btn--primary btn--xl">
              Call {contact.phone}
            </a>
            <a href={`mailto:${contact.email}`} class="btn btn--ghost btn--xl">
              Email Us
            </a>
          </div>

          {/*
            Paste the Vagaro URL into `BOOKING_URL` in src/data/site.ts and this
            block becomes the online-booking version automatically.
          */}
        </>
      )
    }
  </div>
</section>

<style>
  .booking {
    position: relative;
    background:
      radial-gradient(ellipse 80% 90% at 50% 0%, rgba(255, 255, 255, 0.08), transparent 65%),
      var(--surface);
    border-block: 1px solid var(--line);
    overflow: hidden;
  }

  .booking::before {
    content: '';
    position: absolute;
    inset: 0;
    background: repeating-linear-gradient(
      -60deg,
      rgba(255, 255, 255, 0.02) 0 2px,
      transparent 2px 14px
    );
    pointer-events: none;
  }

  .booking__inner {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding-block: clamp(3.5rem, 8vw, 6rem);
  }

  .booking__title {
    max-width: 18ch;
    margin-bottom: 1rem;
  }

  .booking__text {
    max-width: 52ch;
    font-size: clamp(1.02rem, 1.6vw, 1.15rem);
    color: var(--text-muted);
    margin-bottom: 2rem;
  }

  .booking__btns {
    justify-content: center;
  }

  /* Reads as a footnote to the button above it — one step down in size and
     colour, never a second competing call to action. */
  .booking__trial {
    margin: 1.15rem 0 0;
    font-size: 0.95rem;
    color: var(--text-muted);
  }

  .booking__trial a {
    color: var(--text);
    font-weight: 600;
    border-bottom: 1px solid rgba(255, 255, 255, 0.35);
    transition: border-color 0.18s ease;
  }

  .booking__trial a:hover {
    border-bottom-color: #fff;
  }

  .booking__note {
    margin: 0.9rem 0 0;
    font-size: 0.9rem;
    color: var(--text-dim);
    max-width: 46ch;
  }
</style>
